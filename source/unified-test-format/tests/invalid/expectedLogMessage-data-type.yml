description: "expectedLogMessage-data-type"

schemaVersion: "1.11"

createEntities:
  - client:
      id: &client0 "client0"

tests:
  - description: "foo"
    operations: []
    expectLogMessages:
      - client: *client0
        messages:
          - level: debug
            component: command
            data: 0
