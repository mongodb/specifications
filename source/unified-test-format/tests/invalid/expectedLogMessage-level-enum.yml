description: "expectedLogMessage-level-enum"

schemaVersion: "1.11"

createEntities:
  - client:
      id: &client0 "client0"

tests:
  - description: "foo"
    operations: []
    expectLogMessages:
      - client: *client0
        messages:
          - level: foo
            component: command
            data: {}
