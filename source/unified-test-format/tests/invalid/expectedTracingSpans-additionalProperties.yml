description: "expectedTracingSpans-additionalProperties"

schemaVersion: "1.26"

createEntities:
  - client:
      id: &client0 "client0"

tests:
  - description: "additional property foo not allowed in expectTracingSpans"
    operations: []
    expectTracingSpans:
      client: *client0
      ignoreExtraSpans: false
      spans: [{}]
      foo: 0
