description: clientEncryptionOpts-mixed-kms-credentials

schemaVersion: "1.8"

createEntities:
  - client:
      id: &client0 client0
  - encryptedClient:
      id: &encryptedClient0 encryptedClient0
      clientEncryptionOpts:
        keyVaultClient: *client0
        keyVaultNamespace: keyvault.datakeys
        kmsProviders:
          aws: { accessKeyId: "accessKeyId", secretAccessKey: { $$placeholder: {} } }
          azure: { tenantId: "tenantId", clientId: { $$placeholder: {} }, clientSecret: { $$placeholder: {} } }
          gcp: { email: "email", privateKey: { $$placeholder: {} } }

tests:
  - description: ""
    operations: []
