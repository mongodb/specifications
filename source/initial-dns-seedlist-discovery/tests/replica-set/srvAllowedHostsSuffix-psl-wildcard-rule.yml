# "*.nom.br" is a wildcard rule in the Public Suffix List, so "abc.nom.br" is
# matched by it and is a public suffix, and must be rejected. Note that
# "abc.nom.br" does not appear literally in the list.
uri: "mongodb+srv://test1.test.build.10gen.cc/?srvAllowedHostsSuffix=abc.nom.br"
seeds: []
hosts: []
error: true
comment: Should fail because srvAllowedHostsSuffix is a public suffix matched by the wildcard rule "*.nom.br".
